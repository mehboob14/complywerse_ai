# UI Revamp — Change Log (for undo/rollback)

**Branch:** `ui-revamp` (all revamp work lives here; `main` is untouched).
**How to undo everything at once:** `git checkout main` — the app returns to its pre-revamp state instantly. To keep the branch but drop it from what ships, simply don't merge `ui-revamp`.
**How to undo one module:** each module is its own commit (see table). Revert a single commit with `git revert <sha>` (safe, creates an inverse commit) or inspect it with `git show <sha>`.
**Safety rules honored:** presentation-layer only by default (no changes to event handlers, API calls, state, data flow, or validation unless explicitly flagged + approved); data & API contracts intact; no DB/schema changes; backend never restarted by me.

---

## Legend
- **Scope** — `docs` (planning/spec, no app code) · `mockup` (throwaway HTML in /mockups, no app code) · `ui` (presentation-layer app code) · `fn` (functional change — only when audit-flagged AND approved).
- **Revert** — the exact command to undo just that entry.

---

## Phase 1 — Audit (no app code changed)

| # | Date | Scope | Files | What | Revert |
|---|------|-------|-------|------|--------|
| 1.1 | 2026-07-02 | docs | `UI-REVAMP-AUDIT-PHASE-1.md` (new) | Full Phase-1 audit of Governance · Risk · Compliance. No app code touched. | `git rm UI-REVAMP-AUDIT-PHASE-1.md` (or delete file) |

## Phase 2 — Design System (foundations; no app code changed yet)

| # | Date | Scope | Files | What | Revert |
|---|------|-------|-------|------|--------|
| 2.1 | 2026-07-04 | docs | `UI-REVAMP-CHANGES.md` (new) | This change log. | delete file |
| 2.2 | 2026-07-04 | docs | `DESIGN-SYSTEM.md` (new) | Authoritative design-system spec — real tokens (teal `#1ed4b0`, near-black-on-teal), semantic/neutral scales, component inventory + charter-clean flags + 13 gaps to build, pattern library w/ exemplar refs, 15-row DO/DON'T, Snapshot Test. | delete file |
| 2.3 | 2026-07-04 | mockup | `mockups/design-system.html` (new) | Visual living style guide (throwaway) — color ramps, type scale, buttons, chips, snapshot strip/board, register table, master-detail, DO/DON'T, empty state. | delete file |

> Phase 2 defines the system only. **No application code is modified in Phase 2.** Token/component/config changes are proposed here and applied per-module in Phase 3, each as its own revertible commit.

## Phase 3 — Module-by-module revamp

*(entries added per module as work proceeds — each row = one git commit)*

| # | Date | Scope | Module | Files | What | Commit / Revert |
|---|------|-------|--------|-------|------|-----------------|
| 3.16 | 2026-07-04 | ui | Compliance · Controls | `controls/page.tsx` · `controls/overview/page.tsx` (new) · `controls/_shared/components.tsx` (new) | **Split-workbench redesign (per user screenshot).** `/controls` is now a two-pane workbench (12-col): left = search + framework filter + quick chips (All/Gaps/Mine/In-progress) + scrollable control list (code · framework · priority · title · evidence fraction · status dot, selected highlight); right = a **docked inspector** (replaces the old `AnimatedModal` popup) with header + a 4-stage pipeline (Not-started→In-progress→Implemented→Verified, reflecting `control_status`) + owner + Manage-evidence + Requirement + Evidence checklist (`FrameworkControlEvidenceLinkSection`) + Recommended evidence + Mapping + Activity + full AI-recommendations (test procedures / audit focus / addressed-risks close + risks-if-not promote) + Prev/Next. Overview moved to **`/controls/overview`** (health snapshot + framework Tree/Doc/Table views); cross-linked both ways. Shared types/components extracted to `_shared/components.tsx`. **Pipeline/owner read-only** — the framework-controls API has no status/owner-update endpoint (none invented). All queries/mutations/evidence-linking/AI/promote-to-risk preserved. page.tsx 2127→1189. tsc clean. | `git revert <this commit>` |
| 3.19 | 2026-07-04 | ui | Vulnerabilities · Detail (D1) | `vulnerabilities/[id]/page.tsx` | **Vuln detail → Evidence D1 split.** `risk-workspace` wrapper; 12-col: **sticky left rail** (KEV/exploit chips + Triage tile [SeverityBadge · CVSS · EPSS · Priority · SLA countdown] + Identity [VULN/CVE/CWE] + Ownership & **mitigation-progress** + **exception-state** + Timeline + in-page nav), **right scrolling** sections: narrative/threat-intel/patch → description → assets → **Remediation (AI + mitigations co-located)** → controls → chain → exception → activity (workflow+history+escalations merged). Reused `ThreatIntelPanel`/`AIAnalysisTab`/`DependenciesTab` + all 5 modals unchanged; **wired the dead `ExceptionWorkflowPanel` FSM** (replaced thin table) + **removed the orphan retests** tab/query. Tab-gated queries flipped to always-on (same keys/shapes). Charter-cleaned (blue→teal, removed AI-card gradient, shared SeverityBadge/StatusBadge). tsc: only 1 **pre-existing** error (`getAll({limit:500})` in `DependenciesTab`, verbatim in HEAD) — no regression. | `git revert <this commit>` |
| 3.18 | 2026-07-04 | ui | Assets · Detail (D1) | `assets/[id]/page.tsx` · `assets/[id]/_components/TrajectoryMap.tsx` | **Asset detail → Evidence D1 split.** `risk-workspace` wrapper; 12-col grid: **sticky left `AssetContextRail`** (actions + identity + criticality/status/exposure pills + CIA block + Coverage/CIS/Risk/criticality stat rows + ownership chain + lifecycle/classification/last-seen), **right scrolling** section switcher with the **Asset→Vuln→Risk Trajectory graph promoted to first/default** (un-hidden; MiniMap dropped for the flat look) → Vulns → Risks → Controls → Evidence → Compliance (HostApplicationsPanel) → Criticality Assessments → Mapping. **All tab panels/components reused unchanged**; every query/mutation/modal/RoomScanContext + permissions preserved. Charter-cleaned (blue→teal, flat cards, shared status scale). tsc clean. | `git revert <this commit>` |
| 3.17 | 2026-07-04 | ui | Compliance · Controls (refine) | `controls/page.tsx` | **Per user feedback** on the workbench: tightened typography (page `text-2xl`→`text-lg` header, `text-xs` subtitle); **moved Recommended evidence into a popup** (`AnimatedModal`, opened by a "Recommended evidence (N)" button in the inspector action row) to cut per-control scrolling; put **Mapping + Activity side-by-side** (2-col) to match the reference. AI recommendations stay on-demand (button-gated). tsc clean. | `git revert <this commit>` |
| 3.15 | 2026-07-04 | ui | Assets · Workspace | `assets/page.tsx` · `assets/_workspace/*` (7 new) | **Implemented the Assets Workspace** (Register + Workbench). New `_workspace/`: `RowActionsMenu` (copied), `lib.tsx` (asset cell primitives — type/criticality/status pills, CIA meter, owner cell, lifecycle dots, last-seen), `RegisterView` (DataTable: Asset/Type/Owner/Criticality/CIA/Status/Lifecycle/Last-seen/⋯ + bulk-connect + export + sticky), `AssetRow`, `DetailPreview` (fires `asset-detail`/`asset-coverage`; CIA/criticality/coverage/CIS/risk tiles + owner chain + cross-module counts + Open-full), `WorkbenchView` (12-col split), `AssetsWorkspace` shell (KPI strip + toolbar + Register⇄Workbench switch). `AssetsPage` return swapped from 3 charts + gradient guidance + hand-rolled table → `<AssetsWorkspace>`, keeping **every** query/mutation/filter/handler + both `AssetModal`s + `ImportAssetsModal` + the connect/bulk-connect flow. page.tsx 2120→1470. tsc + eslint clean. | `git revert <this commit>` |
| 3.12 | 2026-07-04 | ui | Shared · SeverityBadge | `components/ui/SeverityBadge.tsx` | **Prerequisite for the vuln register.** Converted the dark `bg-severity-*/20` chips to charter **light pills** (critical rose · high orange · medium amber · low emerald · info slate). Improves every current consumer too. | `git revert <commit>` |
| 3.13 | 2026-07-04 | mockup | Assets Workspace | `mockups/assets-workspace-A-register.html` (new) | Register view — Documents-style DataTable (Owner/criticality/CIA/status/lifecycle/last-seen cols, ⋯ menu, KPI strip, view-switcher). Throwaway. | delete file |
| 3.14 | 2026-07-04 | mockup | Assets Workspace | `mockups/assets-workspace-B-workbench.html` (new) | Workbench view — Evidence master-detail (asset list left + sticky preview right: CIA/criticality/coverage/CIS/risk tiles + owner chain + cross-module counts + "Open full record"). Throwaway. | delete file |
| 3.1 | 2026-07-04 | mockup | Governance Overview | `mockups/governance-overview-A-board-snapshot.html` (new) | Direction A — board snapshot (health gauge + KPI strip + ranked attention rail + posture bars). Throwaway, no app code. | delete file |
| 3.2 | 2026-07-04 | mockup | Governance Overview | `mockups/governance-overview-B-command-center.html` (new) | Direction B — command center (6 metric tiles: donut/ring/bars, recolored + consolidated). Throwaway. | delete file |
| 3.3 | 2026-07-04 | mockup | Governance Overview | `mockups/governance-overview-C-focus-rail.html` (new) | Direction C — focus + sticky attention rail (mirrors Committees/Documents). Throwaway. | delete file |
| 3.6 | 2026-07-04 | mockup | Document Templates popup | `mockups/document-templates-A-refined-tabs.html` (new) | Direction A — charter-clean refined tabs (flat teal header, teal active tabs, unified slate card language, per-card action cue, semantic-only chips). Throwaway. | delete file |
| 3.7 | 2026-07-04 | mockup | Document Templates popup | `mockups/document-templates-B-source-rail.html` (new) | Direction B — left source rail + gallery (sources with counts + editor/AI-Draft tags), shown on the Artifact Templates accordion. Throwaway. | delete file |
| 3.11 | 2026-07-04 | ui | Governance · Document detail | `governance/documents/[id]/page.tsx` · `governance/layout.tsx` | **Charter cleanup of the single-document page (per user — "looks inconsistent").** Governance **top tabs** (in `layout.tsx`) and the document **sub-tabs** blue→**teal** active; the purple **"Policy"** badge + blue "Approved"/rainbow status & doc-type maps → **charter semantic/slate pills**; the 4 primary CTAs (white-on-blue) → **near-black-on-teal**; flattened the blue→cyan hero gradient + the dark-theme purple version-history chips; swept **~650** occurrences of `gray→slate`, `text-black→slate-900`, and blue/purple/indigo/violet/cyan accents → teal/slate. Presentation-only (no logic/data touched). Type-checks clean. *(`layout.tsx` tab-color fix applied in the working tree but NOT in this commit — you have an in-progress `Clock` edit in that file; commit it yourself so both land together.)* | `git revert <this commit>` (detail page); layout fix lives in your working tree |
| 3.9 | 2026-07-04 | ui | Governance · Exceptions | `governance/exceptions/page.tsx` | **Register-ify + trim (per user).** Replaced the basic `.table` with the **Documents-style `DataTable` register** (sortable columns, sticky header, CSV export, whole-row → view, shared `RowActionsMenu` ⋯ menu carrying the same lifecycle actions). **Removed both Risk-Posture cards** (the asset-weighted gauge + the posture-trend line) and put the remaining three — Status Distribution · Open Exception Aging · Closure Timeliness — in **one row**. **Moved "Find & generate exceptions" above the search bar.** Kept the same buttons (SearchInput + Status/Priority filters + `btn-primary` New Exception). Charter-cleaned the touched surfaces: status/priority pills → semantic light pills, `gray-*`→slate, indigo→teal in the discover panel, aging bars recolored, and flattened the create-modal "AI is working" gradient. All queries/mutations/handlers preserved (posture computations left intact but unused). Type-checks clean. *(Create/edit + view + approve/reject/revoke modals left for a follow-up modal pass.)* | `git revert <this commit>` |
| 3.8 | 2026-07-04 | ui + fn | Document Templates popup | `governance/documents/_RecommendedDocsModal.tsx` | **Implemented Direction B** (source rail + gallery). Charter-clean rewrite of the whole modal + its 3 preview/detail popups: replaced top tabs with a left **source rail** (each source tagged Editor / ✦ AI Draft); flattened **all gradients** (main + NCA/law/artifact popup headers + artifact accordion header); one teal brand + slate + semantic (retired indigo/violet/blue/emerald-as-brand); `gray-*`→slate; rainbow `DOC_TYPE_BADGE` + metadata chips → neutral slate; scrims `bg-black/50`→`bg-slate-900/50` + `animate-fade-in`; per-card action cue ("Opens in editor" / "AI Draft"); relabeled CTAs ("Use template" / "AI Draft"). **[fn]** replaced the artifact-download `alert()` with a toast. **All queries, filters, `onPick`/`emit` payloads, and the artifact accordion/expand logic preserved verbatim** (incl. restoring the original 2-arg artifact emit). Type-checks clean. | `git revert <this commit>` |
| 3.5 | 2026-07-04 | ui | Governance Overview | `governance/page.tsx` | **De-compliance'd + committee focus** (per user). Removed the Framework-coverage card, the compliance "Coverage" KPI, the framework gap-severity chips, and the framework/coverage from posture bars + attention rail + the blended health score. Added a **Committee oversight** card (meetings / open / overdue action tiles + action-health bar + committee & meeting chips), a **Committees** KPI, a **Committees** posture bar (action health), an **Exception attention** rail row, and a Committees header link (replacing the compliance Statements link). Dropped the now-unused `complianceCoverage` / `complianceByFramework` / `openGapsSummary` queries. Health score rebalanced onto governance-only signals. Type-checks clean. | `git revert <this commit>` |
| 3.4 | 2026-07-04 | ui | Governance Overview | `governance/page.tsx` | **Implemented Direction A** (board snapshot). Presentation-layer reskin — **all 16 `useQuery` hooks, keys, and data flow unchanged**. New layout: proper H1 (`text-sm`→`text-xl`), 5-KPI strip, teal health gauge + 4 signal tiles + 6 posture bars + document-status split, ranked "needs attention now" rail, framework-coverage bars + gap chips, recolored (teal/slate) throughput trend. Removed dead `SpeedometerCard`/`GovernanceSunburst`/`GovernanceBowTie` + their dead data, and the off-brand `DonutChart`/`MiniMetricRing`/`GovernanceHealthRadar`/`LollipopChart` helpers. Added attention-rail nav links to existing routes only. Type-checks clean. | `git revert <this commit>` |

---

## Notes on pre-existing uncommitted work (NOT mine)
At branch creation, the working tree already contained uncommitted changes from prior sessions (documents workspace, controls, `api.ts`, some backend files, `RowActionsMenu.tsx`). **I do not commit these** — my commits use explicit file paths so your in-progress work is never swept in. If you want them preserved separately, commit them on `main` or a branch of your choosing before merging `ui-revamp`.
