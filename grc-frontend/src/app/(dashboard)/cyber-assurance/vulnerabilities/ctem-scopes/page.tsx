'use client';

/**
 * CTEM Scopes & Cycles — the redesigned page (design handoff, wired to live data).
 * The previous card-list page is kept as _legacy_page.tsx.bak for reference.
 */
export { default } from './CtemScopesRedesign';
