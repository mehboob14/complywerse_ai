# Copy to .env next to docker-compose.yml (optional).
# Do not commit real secrets.

METABASE_HOST_PORT=3001
MB_DB_PASSWORD=metabase_dev_change_me

# Same value as backend METABASE_JWT_SHARED_SECRET (openssl rand -hex 32)
METABASE_JWT_SHARED_SECRET=
