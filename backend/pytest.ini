[pytest]
# TPRA productionization test harness (the repo's first backend test suite).
# Run from the backend/ directory:  py -3 -m pytest
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
